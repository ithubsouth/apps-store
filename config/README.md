Update: Added a local feature flags file enabling p2pShare for development.

This file (config/feature-flags.local.json) is intended for local/dev use only and should not be used in production CI/CD. It enables the P2P Share demo UI when the app reads local feature flags.
